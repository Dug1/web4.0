class Layout:
    def __init__(self, options, parent, children=[]):
        self.options = options
        if "width" in options:
            self.width = options["width"]
        else
            self.width = parent.width

        if "height" in options:
            self.width = options["height"]
        else
            self.width = parent.width

    def get_size(self):
        if type(self.width) == float:
            
    def change_size(self):
        
            
